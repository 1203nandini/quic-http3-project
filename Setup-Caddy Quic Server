1. INSTALL CADDY:
sudo apt update
sudo apt install -y debian-keyring debian-archive-keyring apt-transport-https
curl -fsSL https://dl.cloudsmith.io/public/caddy/stable/gpg.key | sudo gpg --dearmor -o /usr/share/keyrings/caddy-stable-archive-keyring.gpg
curl -fsSL https://dl.cloudsmith.io/public/caddy/stable/debian.deb.txt | \
sudo tee /etc/apt/sources.list.d/caddy-stable.list
sudo apt update
sudo apt install -y caddy


2. Configure Caddy file
https://localhost {
    tls internal
    respond "QUIC is running on localhost!"
}
apply config :> sudo systemctl reload caddy


3. Test Quic/HTPP3
curl -I --http3 --insecure https://localhost

4. Wireshark QUIC Packet Analysis
🔍 Capture QUIC Packets

Open Wireshark
Select interface → lo (loopback)
Apply filter: udp.port == 443
Now refresh your browser : https://localhost


